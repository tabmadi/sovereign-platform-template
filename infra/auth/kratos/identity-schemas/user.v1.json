{
  "$id": "https://example.com/identity-schemas/user.v1.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "User",
  "type": "object",
  "properties": {
    "traits": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "title": "E-Mail",
          "ory.sh/kratos": {
            "credentials": { "password": { "identifier": true } },
            "verification": { "via": "email" },
            "recovery": { "via": "email" }
          }
        },
        "name": { "type": "string", "title": "Name" },
        "operator": {
          "type": "boolean",
          "title": "Operator",
          "description": "Grants the coarse ops-tier gate (ADR-0017): with AAL2, this trait reaches the operator dashboards. Absent/false for product users."
        }
      },
      "required": ["email"],
      "additionalProperties": false
    }
  }
}
