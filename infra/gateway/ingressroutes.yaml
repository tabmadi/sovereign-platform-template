# Shared Traefik IngressRoutes (ADR-0305, ADR-0306). Per-service /api/<svc> routes
# are emitted by the service Helm chart (infra/helm/service/templates/ingressroute.yaml)
# on the product apex. These are the cross-cutting OPS-TIER routes.
#
# Trust-tier split (ADR-0306): every operator dashboard lives on its own origin
# `{tool}.ops.<host>`, never on a product path. HostRegexp keeps the manifests
# host-agnostic (env parity, ADR-0205) — `^{tool}\.ops\..+` matches
# {tool}.ops.dev.localtest.me locally and {tool}.ops.<apex> in every deployed env.
# The `*.ops.<host>` wildcard cert (secret wildcard-ops-tls) covers all of them.
#
# Each route is behind the ops forward-auth (Oathkeeper validates an AAL2 operator
# session and the per-tool `dashboard:<tool>#view` grant — ADR-0304/0017) and the
# identity-header strip + security-headers middlewares.
---
# Hubble UI (ADR-0206, ADR-0501) — Cilium network-flow visibility / service map
# (service in kube-system, cross-namespace ref like Argo). Grafana owns detail
# (RED/SLO/logs/traces/profiling); Hubble UI owns the interactive map + drop
# investigation. Known trade-off (ADR-0501): it collapses workloads sharing
# `app.kubernetes.io/name` (all Temporal roles → "temporal").
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-hubble
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^hubble\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: hubble-ui
          namespace: kube-system
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Grafana (ADR-0500) — served at root (serve_from_sub_path dropped, see chart).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-grafana
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^grafana\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: grafana
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Lowdefy internal-admin console (ADR-0401). Oathkeeper injects X-User-Email,
# which Lowdefy keys off (see access-rules ops-lowdefy rule).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-lowdefy
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^lowdefy\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: lowdefy
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Argo CD (ADR-0201). server.insecure is on, so TLS terminates at Traefik and
# argocd-server serves http on :80. Argo is installed into its own `argocd`
# namespace (not `platform`), so the service ref is cross-namespace — the same
# pattern as Hubble UI in kube-system. This needs Traefik allowCrossNamespace, on
# locally via the committed Traefik chart (infra/helm/platform/traefik/values.yaml).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-argocd
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^argocd\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: argocd-server
          namespace: argocd
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Temporal Web UI (ADR-0302) — temporalio/ui at root on :8080.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-temporal
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^temporal\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: temporal-web
          port: 8080
  tls:
    secretName: wildcard-ops-tls
---
# SeaweedFS admin (ADR-0306, ADR-0207) — non-prod only, admin UI at root on
# :23646, and the sole exposed surface of the store. The master and filer UIs are
# diagnostic and are reached by port-forward: an origin per internal view
# multiplies CSP, rate-limit and session surface for no capability this one lacks.
#
# This gate is the only one on the UI: `weed` is started without
# `-admin.password`, so it registers no login (see the chart's `adminAuth`). The
# network policy admits :23646 from the gateway's namespace alone, so the origin
# cannot be reached around the gate from inside.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-seaweedfs
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^seaweedfs\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: seaweedfs
          port: 23646
  tls:
    secretName: wildcard-ops-tls
---
# Headlamp — k8s debug UI (ADR-0501, ADR-0306). Core: the backend Service exists
# in every env; if a project sets headlamp.enabled=false the route resolves to no
# endpoints.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-headlamp
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^headlamp\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: headlamp
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# pgweb — read-only DB inspector (ADR-0401, ADR-0306). Core: the backend Service
# exists in every env; setting pgweb.enabled=false leaves the route with no endpoints.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-pgweb
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^pgweb\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: pgweb
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Mailpit viewer (ADR-0307, ADR-0306) — non-prod only, the sink's UI at root on
# :8025. Production sets mailpit.enabled=false, so the Service is absent there and
# the route resolves to no endpoints, as the SeaweedFS admin route does in prod.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-mailpit
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^mailpit\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: mailpit
          port: 8025
  tls:
    secretName: wildcard-ops-tls
---
# zot's console (ADR-0105, ADR-0306). The SAME pod as the `registry` route below,
# on a second origin with a different gate — which is the whole point of separating
# them. A browser reaching the catalogue is an operator and belongs behind the ops
# chain; a containerd pulling from it is not and cannot pass one.
#
# This origin targets the console-proxy port (5001), not the registry port. The
# proxy adds the pull identity's credential so the operator the ops chain already
# vouched for is not asked for a second login — an anonymousPolicy would smooth it
# over too, but it opens every pull on `registry.<host>` to do it (ADR-0105). The
# registry route below stays on 5000 and reaches zot directly, so the credential is
# added on the console path alone.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-zot
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^zot\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: zot
          port: 5001
  tls:
    secretName: wildcard-ops-tls
---
# The registry (ADR-0105), and the one origin at the edge that is NOT gated on an
# operator session.
#
# Everything else here is a browser tool, so forward-auth to Oathkeeper is the
# right gate. A registry client is not a browser: `docker login`, containerd and
# BuildKit speak the OCI distribution spec's own Basic/Bearer challenge and follow
# no redirect to a login page. Putting the ops chain in front of this route makes
# every push and every pull fail with an HTML redirect the client cannot read.
#
# What guards it instead is zot's own htpasswd (`ci` may push, `cluster` may pull,
# anonymous gets nothing — ADR-0105), which is an authentication the client can
# actually satisfy. That is why the host is `registry.<host>` rather than
# `registry.ops.<host>`: the `ops.` label is the scope of the operator cookie
# (ADR-0306), and an origin that never sees that cookie must not sit inside it.
#
# Pulls BY THE CLUSTER do not come through here at all — a kubelet reaches the
# Service directly, which is what the `host`/`remote-node` ingress allow in the
# network policy is for. This route exists for the pipeline that pushes, and for a
# person debugging what is in the registry.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: registry
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^registry\..+`)
      middlewares:
        - name: security-headers
      services:
        - name: zot
          port: 5000
  tls:
    secretName: wildcard-tls
---
# The identity API (ADR-0304, ADR-0305). Kratos's self-service endpoints, on the
# PRODUCT origin under `/auth`, because the session cookie is the product origin's.
#
# This route lived only in the local tier's edge file, so a deployed environment
# had a login PAGE and nowhere for it to submit: the page redirects the browser to
# `/auth/self-service/login/browser` to start a flow, and that path 404'd — which
# reads as a broken frontend rather than a missing route. Every tier syncs this
# directory, so it belongs here.
#
# The UI pages under `/auth/login` and friends are deliberately NOT matched: they
# are the frontend's, and fall through to it.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kratos-public
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: PathPrefix(`/auth/self-service`) || PathPrefix(`/auth/.well-known`) || PathPrefix(`/auth/sessions`)
      middlewares:
        # Rate-limited: this is the surface a credential-stuffing attempt aims at.
        - name: strip-auth
        - name: auth-ratelimit
        - name: security-headers
      services:
        - name: ory-kratos-public
          port: 80
  tls:
    secretName: wildcard-tls
