# Internal-admin policy (ADR-0009, ADR-0012). Routes /internal/admin/* to Lowdefy.
apiVersion: tyk.tyk.io/v1alpha1
kind: SecurityPolicy
metadata: { name: internal-admin, namespace: tyk }
spec:
  name: internal-admin
  active: true
  rate:   500
  per:    60
  quota_max: -1
