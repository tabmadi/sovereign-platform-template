# Dashboards-as-code (ADR-0500). This directory is a chart so that the dashboard JSON beside this file becomes the
# `grafana-dashboards` ConfigMap the Grafana chart mounts (observability values: dashboardsConfigMaps.default).
apiVersion: v2
name: grafana-dashboards
description: Committed Grafana dashboards, delivered as the ConfigMap the Grafana chart mounts (ADR-0500).
type: application
version: 0.1.0
