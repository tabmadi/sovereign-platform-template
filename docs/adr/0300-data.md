# ADR-0300: Data & Migrations

- **Status:** Accepted
- **Date:** 2026-08-06
- **Deciders:** Platform team
- **Related:** [ADR-0000](0000-platform-foundations.md), [ADR-0100](0100-language-and-runtime.md), [ADR-0200](0200-cluster-topology.md), [ADR-0201](0201-gitops.md), [ADR-0202](0202-secrets.md), [ADR-0301](0301-data-lifecycle-privacy.md), [ADR-0302](0302-temporal.md), [ADR-0303](0303-api-contracts-and-lifecycle.md), [ADR-0304](0304-identity-and-authorization.md)
- **Decides:** One Postgres cluster holds one logical database per service, with sqlc queries and dbmate migrations, and no service reaches another's data.

## Context

Every backend service needs persistent state. This ADR answers engine, operation, schema evolution, service access, and parity in one place.

Several platform components already require Postgres: Kratos and Hydra, OpenFGA, and Temporal. A second engine for application services would mean operating two.

Authz-relevant mutations are workflows ([ADR-0302](0302-temporal.md)), so the database needs no built-in outbox.

## Decision drivers

1. **One engine.** Polyglot persistence is a permanent operational tax multiplied by the service count ([ADR-0000](0000-platform-foundations.md)).
2. **Service-owned data.** A service's data is private; other services read it only through that service's HTTP API ([ADR-0000](0000-platform-foundations.md), principle 10).
3. **A schema change is reviewed as a diff**, in the same pull request as the code that depends on it.
4. **Parity.** Whatever produces a working database in production works locally with one command, from the same artefact.
5. **No extension becomes tribal knowledge.** Extensions are per-service decisions recorded in the service README.
6. **A tenant's rows cannot be read by another tenant even when application code is wrong.**

## Considered options

### Engine

The store holds customer data, so it is the most expensive exit on the platform ([ADR-0000](0000-platform-foundations.md), principle 4) and it is compared at full depth.

| Option | Already required by the platform | Distributed by default | What it costs at this shape | Verdict |
| --- | --- | --- | --- | --- |
| **PostgreSQL** | **yes** — Kratos, Hydra, OpenFGA, Temporal | no, and a read replica is the growth path | nothing beyond the cluster the platform components already need | **Chosen.** Strong consistency, transactional DDL, mature replication and backup tooling, an extension ecosystem covering anticipated needs, and the best Go driver with first-class sqlc support *(documented)* |
| MySQL / MariaDB | no | no | a second engine to operate beside the one the platform components already need *(reasoned)* | Adds an engine for no capability gained. `NUMERIC` and transactional DDL are the two rows where it is behind, and neither is why it loses |
| CockroachDB | no | yes, and it is the point | multi-node consensus on every write, and a PostgreSQL wire-protocol compatibility surface that is close rather than equal *(documented)* | Solves horizontal write scaling and multi-region residency. Neither is a requirement here, and the platform components above are tested against PostgreSQL rather than against a compatible dialect |
| YugabyteDB | no | yes | as CockroachDB, with a genuine PostgreSQL query layer on a distributed storage engine | The strongest of the distributed options on compatibility, and the same answer: the capability being bought is one this axis position does not need, paid for on every write |
| SQLite | no | no | none — it is a file | Correct at axis A low. At axis A high the store is shared by several deployables, which is the one thing it cannot do |
| A document store beside Postgres | no | varies | polyglot persistence pays operational cost per engine | Rejected on principle 5. `jsonb` covers the document shapes this platform has |

### Cluster operator

| Option | Kubernetes-native CRDs | Pooler included | Verdict |
| --- | --- | --- | --- |
| **[CloudNativePG](https://cloudnative-pg.io/documentation/current/)** | `Cluster`, `Pooler`, `Backup`, `ScheduledBackup` | **yes**, as a CRD | **Chosen.** A single-binary operator, HA via streaming replication, failover and switchover as CRD operations, backups to object storage with PITR via Barman. Apache-2.0, CNCF Sandbox — the least mature governance signal of the four, and principle 4 records it rather than hiding it: the exit is a `pg_dump` and a different operator, because the data is plain Postgres *(documented)* |
| Zalando postgres-operator | yes | separate | Mature, and its configuration surface is larger for the same capability |
| Crunchy PGO | yes | separate | Capable, with a heavier component set |
| Percona Operator for PostgreSQL | yes | **yes** | A [hard fork of Crunchy PGO](https://www.percona.com/blog/percona-operator-for-postgresql-3-0-0-hard-fork-olm-scoping-major-upgrades/) carrying Percona's Postgres distribution and its own monitoring stack, which is a second one beside [ADR-0500](0500-observability.md)'s |
| StackGres | yes | yes | Broadest feature set and the largest operational surface of the four |
| Raw Patroni | **no** — not Kubernetes-native | no | Every CRD operation above becomes a runbook |

### SQL access

| Option | Query plans visible | Runtime cost | Verdict |
| --- | --- | --- | --- |
| **sqlc + pgx** | **yes — the SQL is hand-written** | none; generated Go | **Chosen** *(reasoned)* |
| GORM, ent, bun | hidden behind an abstraction | reflection or builder overhead | Hiding query plans becomes a liability exactly when load makes them matter |
| sqlx | **yes — the SQL is hand-written** | reflection, per row scanned | The same authored SQL as the chosen option, with the result shape checked at runtime rather than at compile time. sqlc reads the schema and generates the struct instead |
| `database/sql` by hand | yes | none | sqlc produces the same thing with types, for free |

### Migrations

| Option | Schema is authored as | Runs as | Added toolchain | Verdict |
| --- | --- | --- | --- | --- |
| **dbmate** | plain SQL, one file per change, with `up` and `down` sections | one static binary | none — a Go binary, and the same one locally and in the init container | **Chosen.** Driver 3 in its most literal form: the review artefact is the SQL that will run *(reasoned)* |
| golang-migrate | plain SQL, two files per change | a binary or a Go library | none | Equivalent in every respect that matters here, and it splits each change across two files instead of one |
| goose | SQL or Go migrations | a binary or a library | none | Go migrations are the differentiator, and a backfill is a Temporal workflow here rather than a migration |
| Atlas | a **declarative desired-state** schema, with the migration computed | a binary, and a plan step | none, though [parts of the workflow are a paid tier](https://atlasgo.io/pricing) | The strongest alternative and the closest call: a declarative schema is a better review artefact than a diff of DDL. Rejected because the generated plan, not the authored file, is what runs — so the thing reviewed and the thing executed are not the same object |
| Flyway or Liquibase | SQL or XML/YAML changelogs | a JVM | **a JVM in the init container** | [ADR-0100](0100-language-and-runtime.md) bars the runtime, and the changelog formats are a second schema language |
| Hand-run `psql` scripts — the honest baseline | plain SQL | whoever runs it | none | Nothing records which scripts an environment has had applied |

### Multi-tenancy

| Option | Isolation when application code is wrong | Migration matrix | Cost per tenant | Verdict |
| --- | --- | --- | --- | --- |
| **`org_id` column plus Row-Level Security** | **the database refuses the row** | one schema | a row | **Chosen.** Driver 6 needs a guarantee below the application, and RLS is the only option that puts one there without multiplying the schema *(reasoned)* |
| `org_id` column, filtered in application code only | none — a missing `WHERE` returns every tenant | one schema | a row | The failure is silent, and it is the single most common multi-tenant data breach |
| Schema per tenant | strong, by search path | **one migration run per tenant** | a schema, plus catalogue bloat past a few thousand | The matrix explodes, and every migration becomes an N-way partial-failure problem |
| Database per tenant | strongest | one run per database | a database and a connection pool | The isolation a regulated tenant needs, at a cost that only that tenant justifies. Such a tenant earns its own service and cluster instead |

## Decision

### Topology: one logical database per service, one shared cluster

| Rule | Consequence |
| --- | --- |
| Service A cannot connect to service B's database | Credentials are per service |
| No cross-database queries | Cross-service data needs go over HTTP ([ADR-0303](0303-api-contracts-and-lifecycle.md)) |
| One shared physical cluster | Operational cost stays proportional to clusters, not services. Read replicas arrive when single-service load justifies them |
| Compliance isolation earns its own cluster | Never its own schema in the shared cluster |

### Connection pooling: PgBouncer in transaction mode

Deployed through CNPG's `Pooler` CRD, between every service and the cluster.

| Option | How it is deployed here | Picked over it because |
| --- | --- | --- |
| **PgBouncer** | a CNPG `Pooler` CRD — the operator already owns its lifecycle | the pooler is a resource the chosen operator renders, so adopting it costs a values block rather than a component *(reasoned)* |
| pgcat | a chart of its own | Adds sharding and load-balancing this topology does not have, and leaves the CRD path |
| Odyssey | a chart of its own | Stronger multi-threaded throughput, at a component the operator does not manage |
| Application-side pooling only | nothing | `pgx` already pools per replica. What it cannot do is bound the *total* server-side connections across replicas, which is the number Postgres limits |

- **Transaction mode is the default.** Session-mode features — advisory locks, `SET LOCAL`, cross-statement `LISTEN/NOTIFY`, server-side prepared statements without compatibility flags — are not permitted in application code.
- `pgx` sets `DefaultQueryExecMode = QueryExecModeDescribeExec` in `libs/go/dbmw`, which describes a statement without creating a server-side prepared statement, so a transaction-mode pooler is safe.
- Per-service connection limits are sized in PgBouncer, not in `pgx`. The app-side pool is small per replica, and PgBouncer multiplexes onto a smaller server-side pool.

**A session-mode `Pooler` is deferred.**

| Field | Value |
| --- | --- |
| **Trigger** | a service needs `LISTEN/NOTIFY` across statements, an advisory lock, or a session-scoped `SET` |
| **Seam** | ✓ `Pooler` is a CRD and a service reaches its pooler by hostname, so a second one in session mode is a manifest plus one changed connection string. Nothing in application code moves |
| **Cost if adopted late** | the service that needed it has already worked around the absence, usually with polling, and that workaround is what has to be removed |

### Migrations: dbmate

Files live at `services/<service>/migrations/<YYYYMMDDHHMMSS>_<description>.sql`, one SQL file with `-- migrate:up` and `-- migrate:down` sections.

**Down migrations are not used in production.** Production rollback is a forward fix — a new migration reversing the change. Down sections exist for local development.

| Environment | How they run |
| --- | --- |
| Production | `dbmate up` as an **init container** before the service container in the shared chart ([ADR-0201](0201-gitops.md)). The service starts only after it succeeds, and an advisory lock prevents concurrent runs across replicas |
| Local | the same binary against the local database, via `mise run db:migrate` |

The init container image is the one engineers run locally. There is no separate migration image to drift.

SQL is linted by **sqruff** — a single Rust binary, sqlfluff-compatible, reading `.sqlfluff` with `dialect = postgres` — on both queries and migrations, at pre-commit and in CI. It needs no Python toolchain ([ADR-0100](0100-language-and-runtime.md)).

### The OpenFGA schema is a seed Job

Not SQL, and the same class of problem: a declarative schema that must be applied before consumers rely on it.

An idempotent, Argo-synced Job in the OpenFGA chart runs a sync wave after the Deployment is Ready. It writes the canonical schema and touches the **static** ops-policy grants. Both operations are idempotent, so the Job is safe on every sync, and local and deployed environments share one seeding path.

Per-human operator membership is **not** baked in here; it is runtime state managed by `mise run ops:grant` ([ADR-0306](0306-trust-tiers-and-urls.md)).

### Zero-downtime schema changes: expand, migrate, contract

1. **Expand.** Add columns or tables, nullable or defaulted. Deploy.
2. **Migrate data.** Backfills run as **Temporal workflows** ([ADR-0302](0302-temporal.md)), not SQL scripts, so they are observable, resumable, and rate-limitable. Deploy if application code needs new behaviour.
3. **Contract.** Drop old columns or tables only once no code references them.

| Hard rule | Reason |
| --- | --- |
| No destructive migration in the same deploy as code using the new schema | At least one deploy boundary must separate expand from the code switch |
| No long-lock `ALTER TABLE` without `lock_timeout` and a documented online-DDL plan | A long lock is an outage, and it is blocked at review |
| No `DROP COLUMN` until code has shipped without referencing it for one full production deploy cycle | The rollback target must still work |

### Multi-tenancy is row-level

Every multi-tenant table carries an `org_id` column and service code filters by the value from the request context. **Row-Level Security** is enabled on those tables, keyed on `CURRENT_SETTING('app.org_id')`, set per request through `pgx`'s `BeforeAcquire` hook.

RLS is enabled per service the first time that service becomes multi-tenant, rather than on every table by default.

| Field | Value |
| --- | --- |
| **Trigger** | a service's tables begin carrying rows belonging to more than one organisation |
| **Seam** | ✓ the `org_id` column and the `app.org_id` setting are the schema and connection conventions from the start, so enabling RLS is a migration adding a policy — no data moves and no query changes |
| **Cost if adopted late** | the table already holds several tenants' rows, so the policy is enabled against live multi-tenant data, and any query that was silently cross-tenant starts returning nothing instead of too much |

A tenant requiring physical isolation earns its own service, and possibly its own cluster.

### Extensions

| Tier | Extensions | How they arrive |
| --- | --- | --- |
| Cluster default | `pgcrypto`, `uuid-ossp`, `pg_stat_statements` | pre-allowed in the cluster manifest; common enough to be cheap |
| Sanctioned per service | PostGIS, TimescaleDB, pgvector | the first service that needs one installs it in a migration, records it in its README, and the extension joins the cluster's pre-allow list |
| Anything else | — | requires an ADR |

### Credentials and backups

Each service has a dedicated Postgres role with access only to its own database. Roles, grants, and passwords live in a SOPS-encrypted file ([ADR-0202](0202-secrets.md)) that the operator materialises as Secrets the chart mounts. The local connection string is committed in `.env.example` with non-secret defaults.

Backups are [ADR-0207](0207-cluster-storage.md)'s: CNPG writes to the off-cluster bucket with WAL-based PITR, and the restore is rehearsed quarterly. There are no service-specific exceptions.

### Major-version upgrades

A new `Cluster` is created at the new version, CNPG replicates from the old, and traffic cuts over with a brief read-only window per service. Performed once per Postgres release cadence during a planned maintenance window, per [`docs/guide/postgres-major-upgrade.md`](../guide/postgres-major-upgrade.md).

## Consequences

### Positive

- One engine, one mental model.
- Service ownership is structural: a service cannot read another's data even if it tried.
- Migrations live with the code they describe, so one diff carries behaviour and schema together.
- Local matches production: the same binary, the same SQL, the same init-container shape.
- Backfills as workflows make zero-downtime schema change a normal review item rather than an event.

### Negative / Risks

- **Transaction mode rules out a narrow set of Postgres features.** Mitigated by lint and the session-mode seam.
- **A shared physical cluster means a noisy neighbour affects every service.** Mitigated by CNPG resource isolation, per-role slow-query alerting, and per-service connection limits.
- **No ORM is unfamiliar** to engineers arriving from Django, Rails, or Spring. Paid once, during onboarding.
- **Forward-fix as the only rollback path requires discipline.** Documented in [`docs/guide/database-migrations.md`](../guide/database-migrations.md).
- **RLS is opt-in per service**, so a service that becomes multi-tenant without enabling it has tenant isolation only in application code. The `org_id` column is the visible marker a reviewer checks against.

## Rules

- Every service uses Postgres. Another engine requires its own ADR.
- Each service owns exactly one logical database, with credentials scoped to it. Cross-database queries are not used.
- Cross-service data access is HTTP through the generated OpenAPI client. `(CI: ci:lint)`
- The cluster is operated by CloudNativePG, with pooling through a transaction-mode `Pooler`.
- Application code is transaction-mode safe: no advisory locks, no `SET LOCAL`, no cross-statement `LISTEN/NOTIFY`, and no server-side prepared statements without compatibility flags. `(CI: lint:sql)`
- SQL access is sqlc plus pgx. ORMs are not used. `(CI: ci:lint)`
- Migration files live at `services/<service>/migrations/` and are managed by dbmate. `(CI: lint:service-contract)`
- Migrations run as the service's init container in production, and the same binary runs locally.
- Down migrations are local-only. Production rollback is a forward fix.
- Schema changes follow expand, migrate as a Temporal workflow, then contract, with at least one production deploy boundary between expand and the code switch.
- A multi-tenant service's tenant tables carry `org_id` and have RLS enabled keyed on `app.org_id`.
- Sanctioned extensions are PostGIS, TimescaleDB, and pgvector, plus the cluster default set. Others require an ADR.
- Database credentials reach pods through the sops-operator from encrypted files.
