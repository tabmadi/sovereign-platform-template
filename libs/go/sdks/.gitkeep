# Generated per-service Go server + client land here as
# `<service>/server/server.gen.go` and `<service>/client/client.gen.go`.
# Regenerated by `mise run gen:openapi` (ADR-0008). Committed to the repo.
