apiVersion: v2
name: postgres
description: CloudNativePG operator + a shared cluster CR (ADR-0007).
type: application
version: 0.1.0
appVersion: "1.24.1"

dependencies:
  - name: cloudnative-pg
    version: "0.23.0"
    repository: https://cloudnative-pg.github.io/charts
