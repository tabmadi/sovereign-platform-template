# Settings shared by every service in the dev environment (ADR-0201). Layered before each service's own values
# file, so anything here is a default a service may override rather than a rule it cannot.
imagePullSecrets:
  - name: registry-pull
