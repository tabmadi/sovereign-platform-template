apiVersion: v2
name: temporal
description: Self-hosted Temporal cluster (ADR-0006), Postgres-backed via CNPG.
type: application
version: 0.1.0
appVersion: "1.25.2"

dependencies:
  - name: temporal
    version: "0.47.0"
    repository: https://go.temporal.io/helm-charts
