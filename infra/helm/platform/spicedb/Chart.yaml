apiVersion: v2
name: spicedb
description: |
  SpiceDB authorization engine (ADR-0010), Postgres-backed. Deployed directly as
  a Deployment + Service with a `spicedb migrate head` initContainer — no operator
  or CRD. See ADR-0010 for why we skip the operator.
type: application
version: 0.1.0
appVersion: "1.54.0"
