// GENERATED by `mise run gen:zod` — do not edit.
//
// Zod schemas for the request bodies in catalog/openapi.yaml (ADR-0400). Forms
// validate with these through the shared <Form> primitive, so the rule a field
// enforces in the browser is the rule the spec states.
import { z } from "zod";

export const moneySchema = z.object({
  amount: z.string().regex(/^-?[0-9]+(\.[0-9]+)?$/),
  currency: z.string().regex(/^[A-Z]{3}$/),
});

export const productInputSchema = z.object({
  name: z.string().min(1),
  price: moneySchema,
});

export const createProductSchema = productInputSchema;
export type CreateProductInput = z.infer<typeof createProductSchema>;

export const updateProductSchema = productInputSchema;
export type UpdateProductInput = z.infer<typeof updateProductSchema>;
