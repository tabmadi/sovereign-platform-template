all:
  children:
    servers:
      hosts:
        # Populated from Terraform output: terraform output -raw node_ips
        # k3s-dev-1: { ansible_host: x.x.x.1 }
        # k3s-dev-2: { ansible_host: x.x.x.2 }
        # k3s-dev-3: { ansible_host: x.x.x.3 }
  vars:
    ansible_user: root
    ansible_ssh_private_key_file: ~/.ssh/id_ed25519
