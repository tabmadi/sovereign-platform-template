apiVersion: v2
name: cilium
description: CNI + network policy + Hubble observability (ADR-0206).
type: application
version: "2026.9.0"
appVersion: "1.19.5"

dependencies:
  - name: cilium
    version: "1.19.5"
    repository: https://helm.cilium.io
