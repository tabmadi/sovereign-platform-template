# Shared Traefik IngressRoutes (ADR-0305, ADR-0306). Per-service /api/<svc> routes are emitted by the service Helm
# chart (infra/helm/service/templates/ingressroute.yaml) on the product apex.
---
# Hubble UI (ADR-0206, ADR-0501) — Cilium network-flow visibility / service map (service in kube-system,
# cross-namespace ref like Argo).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-hubble
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^hubble\.ops\..+`)
      middlewares:
        # First in the chain: middlewares wrap the ones after them, so a denial
        # raised by forward-auth below is only visible to something listed above
        # it (ADR-0305).
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: hubble-ui
          namespace: kube-system
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Grafana (ADR-0500) — served at root (serve_from_sub_path dropped, see chart).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-grafana
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^grafana\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: grafana
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Lowdefy internal-admin console (ADR-0401). Oathkeeper injects X-User-Email,
# which Lowdefy keys off (see access-rules ops-lowdefy rule).
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-lowdefy
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^lowdefy\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: lowdefy
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Argo CD (ADR-0201). server.insecure is on, so TLS terminates at Traefik and argocd-server serves http on :80.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-argocd
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^argocd\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: argocd-server
          namespace: argocd
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Temporal Web UI (ADR-0302) — temporalio/ui at root on :8080.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-temporal
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^temporal\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: temporal-web
          port: 8080
  tls:
    secretName: wildcard-ops-tls
---
# SeaweedFS admin (ADR-0306, ADR-0207) — non-prod only, admin UI at root on :23646, and the sole exposed surface
# of the store.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-seaweedfs
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^seaweedfs\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: seaweedfs
          port: 23646
  tls:
    secretName: wildcard-ops-tls
---
# Headlamp — k8s debug UI (ADR-0501, ADR-0306). Core: the backend Service exists
# in every env; if a project sets headlamp.enabled=false the route resolves to no
# endpoints.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-headlamp
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^headlamp\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: headlamp
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# pgweb — read-only DB inspector (ADR-0401, ADR-0306). Core: the backend Service
# exists in every env; setting pgweb.enabled=false leaves the route with no endpoints.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-pgweb
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^pgweb\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: pgweb
          port: 80
  tls:
    secretName: wildcard-ops-tls
---
# Mailpit viewer (ADR-0307, ADR-0306) — non-prod only, the sink's UI at root on
# :8025. Production sets mailpit.enabled=false, so the Service is absent there and
# the route resolves to no endpoints, as the SeaweedFS admin route does in prod.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-mailpit
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^mailpit\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: mailpit
          port: 8025
  tls:
    secretName: wildcard-ops-tls
---
# zot's console (ADR-0105, ADR-0306). The SAME pod as the `registry` route below, on a second origin with a
# different gate — which is the whole point of separating them.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ops-zot
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^zot\.ops\..+`)
      middlewares:
        - name: problem-json-errors
        - name: strip-identity-headers
        - name: oathkeeper-forward-auth
        - name: security-headers
      services:
        - name: zot
          port: 5001
  tls:
    secretName: wildcard-ops-tls
---
# The registry (ADR-0105), and the one origin at the edge that is NOT gated on an operator session. Everything
# else here is a browser tool, so forward-auth to Oathkeeper is the right gate.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: registry
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: HostRegexp(`^registry\..+`)
      middlewares:
        - name: security-headers
      services:
        - name: zot
          port: 5000
  tls:
    secretName: wildcard-tls
---
# The identity API (ADR-0304, ADR-0305). Kratos's self-service endpoints, on the PRODUCT origin under `/auth`,
# because the session cookie is the product origin's.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kratos-public
  namespace: platform
spec:
  entryPoints: [websecure]
  routes:
    - kind: Rule
      match: PathPrefix(`/auth/self-service`) || PathPrefix(`/auth/.well-known`) || PathPrefix(`/auth/sessions`)
      middlewares:
        # Rate-limited: this is the surface a credential-stuffing attempt aims at.
        - name: strip-auth
        - name: auth-ratelimit
        - name: security-headers
      services:
        - name: ory-kratos-public
          port: 80
  tls:
    secretName: wildcard-tls
