# Default umbrella values — everything ON. Local profiles override.
certManager:    { enabled: true }
postgres:       { enabled: true }
temporal:       { enabled: true }
tyk:            { enabled: true }
ory:            { enabled: true }
spicedb:        { enabled: true }
observability:  { enabled: true }
minio:          { enabled: true }
sopsOperator:   { enabled: true }
lowdefy:        { enabled: false }   # only when admin app is built
