// GENERATED by `mise run gen:zod` — do not edit.
//
// Zod schemas for the request bodies in authz/openapi.yaml (ADR-0400). Forms
// validate with these through the shared <Form> primitive, so the rule a field
// enforces in the browser is the rule the spec states.
import { z } from "zod";

export const authorizeRequestSchema = z.object({
  aal: z.string(),
  operator: z.string(),
  subject: z.string(),
  tool: z.string(),
});

export const identityUpdateSchema = z.object({
  name: z.string().optional(),
  operator: z.boolean().optional(),
});

export const operatorInputSchema = z.object({
  email: z.string().min(1),
  password: z.string().min(1),
});

export const relationCheckSchema = z.object({
  object: z.string().min(1),
  relation: z.string().min(1),
  subject: z.string().min(1),
});

export const authorizeSchema = authorizeRequestSchema;
export type AuthorizeInput = z.infer<typeof authorizeSchema>;

export const checkRelationSchema = relationCheckSchema;
export type CheckRelationInput = z.infer<typeof checkRelationSchema>;

export const createOperatorSchema = operatorInputSchema;
export type CreateOperatorInput = z.infer<typeof createOperatorSchema>;

export const updateIdentitySchema = identityUpdateSchema;
export type UpdateIdentityInput = z.infer<typeof updateIdentitySchema>;
