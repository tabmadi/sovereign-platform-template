apiVersion: v2
name: cert-manager
description: TLS via cert-manager + Let's Encrypt DNS-01 (ADR-0003).
type: application
version: 0.1.0
appVersion: "1.20.3"

dependencies:
  - name: cert-manager
    version: "v1.20.3"
    repository: https://charts.jetstack.io
