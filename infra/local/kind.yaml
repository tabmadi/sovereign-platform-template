# Local inner-loop cluster (ADR-0600). kind runs upstream Kubernetes on a kubeadm node, which is the closest local
# shape to the distribution the full tier runs.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            registry-qps: "0"
            registry-burst: "0"
            eviction-hard: "imagefs.available<2%,nodefs.available<2%"
            image-gc-high-threshold: "98"
            image-gc-low-threshold: "80"
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8080
      - containerPort: 30443
        hostPort: 8443
networking:
  disableDefaultCNI: true
  kubeProxyMode: none
# The local registry (ADR-0205 parity): a zot container on the kind docker network (created by
# scripts/cluster.sh).
containerdConfigPatches:
  - |-
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."registry.localhost:5000"]
      endpoint = ["http://registry.localhost:5000"]
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."docker.io"]
      endpoint = ["http://registry.localhost:5000"]
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."quay.io"]
      endpoint = ["http://registry.localhost:5000"]
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."ghcr.io"]
      endpoint = ["http://registry.localhost:5000"]
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."registry.k8s.io"]
      endpoint = ["http://registry.localhost:5000"]
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."public.ecr.aws"]
      endpoint = ["http://registry.localhost:5000"]
    [plugins."io.containerd.grpc.v1.cri".registry.mirrors."reg.kyverno.io"]
      endpoint = ["http://registry.localhost:5000"]
