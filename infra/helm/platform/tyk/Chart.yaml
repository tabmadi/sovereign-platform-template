apiVersion: v2
name: tyk
description: Tyk Gateway OSS + operator (ADR-0009).
type: application
version: 0.1.0
appVersion: "5.7.0"

dependencies:
  - name: tyk-stack
    version: "2.4.0"
    repository: https://helm.tyk.io/public/helm/charts/
    condition: tyk.gateway.enabled
